- `.autotune`フォルダを作成する
- パラメータの範囲ごとにフォルダを作成する
    - パラメータの範囲ごとにフォルダ名を一意にする
- rootをそのまま各フォルダにコピーする
- 各フォルダごとに入力を生成する
- 実行ごとに同じフォルダがあるならそれを使い回す
- 実行時はbackendを選べるようにする
    - local、docker、gcなど

1. フォルダごとの入力生成
2. フォルダを指定して実行
- 入力: フォルダの集合
3. 結果をまとめる
- 入力: フォルダの集合
